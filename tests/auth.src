fn scanf(mesg: ptr i8, ...) -> i32;
fn strcmp(a: ptr i8, b: ptr i8) -> i32;
fn printf(mesg: ptr i8, ...) -> i32;

fn main(ac: i32, av: ptr ptr i8) -> i32
{
    let userid: i8[5];
    let userptr: ptr i8 = userid;
    scanf("%s", userid);
    printf("%s\n", userid);
    ret 0;
}