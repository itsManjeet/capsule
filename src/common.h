#ifndef SRCLANG_COMMON_H
#define SRCLANG_COMMON_H

#include <stdbool.h>
#include <stddef.h>
#include <stdint.h>

#define SRCLANG_MAX(a, b) ((a) > (b) ? (a) : (b))
#define SRCLANG_UNUSED(x) ((void)(x))

#endif
