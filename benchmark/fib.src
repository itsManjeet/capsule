native time () int;

let start = time ();
let fib = fun (n) {
    if n <= 1 {
        return n;
    }

    return fib (n-1) + fib (n-2);
};

println (fib (35));

let end = time();
println ("Time: ", end - start);