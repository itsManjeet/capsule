(begin
  (defmacro (while condition . body)
    `(let loop ()
       (cond (,condition
               (begin . ,body)
               (loop)))))

  (define counter 10)
  (while (> counter 0)
    (write 1 "{}\n" counter))
  )