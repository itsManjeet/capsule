import "file";
import "sys";

if f := file.Open("build.src", "r"); type(f) == error {
    println("failed to open build.src, ", f);
    sys.Exit(1);
}

defer { file.Close(); };

data := file.Read();

println (data);