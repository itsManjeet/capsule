import "path";

let Builder = fun (data) {
    let self = clone(data);

    // verification
    for key in ["id", "version", "about"] {
        if not self.search(key) {
            return error("verification failed, missing required key '" + key + "'");
        }
    };

    self.Build = fun (filepath, args) {
        self.commands = [];
        if path.Exists(path.Join(filepath, "bin")) {

        }
    };

    return self;
};